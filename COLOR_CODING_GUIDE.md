# Полное руководство по цветной маркировке сделок

---

# Часть 1: Краткая сводка

## Главное правило

**Каждая пара (покупка → продажа) = уникальный цвет**

---

## Ключевые факты

### ✅ Что показывается:
- Покупка может иметь **несколько** цветов (если используется в нескольких продажах)
- Продажа может иметь **несколько** цветов (если использует несколько покупок)
- Квадратики слева, количество справа

### ❌ Что НЕ показывается:
- Серые строки (не релевантные для целевого года)
- Пары, где хотя бы одна сделка не релевантна
- Начальные остатки (по дизайну)

---

## ⚠️ ВАЖНО: "Релевантна для целевого года" ≠ "Совершена в целевом году"

| Тип сделки | Условие релевантности |
|------------|----------------------|
| **Продажа** | Совершена **В** целевом году (`event_datetime_obj.year == target_report_year`) |
| **Покупка** | **ИСПОЛЬЗУЕТСЯ** в продажах целевого года (даже если покупка из прошлого!) |
| **Начальный остаток** | Используется в продажах целевого года |

**Пример:**
```
Покупка 2023-11-28 (серая строка) → используется в продаже 2025-08-14
                                  → РЕЛЕВАНТНА для 2025!
                                  → Получает цвет ✓
```

---

## Алгоритм (упрощённо)

```
1. Установить is_relevant_for_target_year для всех сделок
   ├─ Продажи целевого года → релевантны
   └─ Покупки из used_buy_ids_for_target_year → релевантны

2. Создать словарь trade_id_to_relevant (быстрый поиск)
   └─ Включая original_trade_ids для агрегированных

3. Для каждой релевантной продажи:
   └─ Для каждого buy_id в used_buy_ids:
      └─ Если buy тоже релевантна:
         └─ pair_to_color[(buy_id, sell_id)] = новый_цвет

4. Для каждой покупки:
   └─ Найти все пары (этот_buy_id, *)
   └─ Собрать цвета

5. Для каждой продажи:
   └─ Найти все пары (*, этот_sell_id)
   └─ Собрать цвета
```

---

## Порядок выполнения (КРИТИЧНО!)

```
❌ НЕПРАВИЛЬНО (было раньше):
   Агрегация → Цветовая маркировка → Установка is_relevant → Return

✅ ПРАВИЛЬНО (сейчас):
   Агрегация → Установка is_relevant → Цветовая маркировка → Return
                ↑ Сначала это!        ↑ Потом это!
```

**Почему важно:** Если порядок нарушен, все проверки `is_relevant_for_target_year` возвращают `False` → цвета не создаются.

---

## Код-локации (быстрая навигация)

| Что | Где |
|-----|-----|
| Установка `is_relevant` | [FFG_ndfl.py:1438-1517](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1438-L1517) |
| Цветовая маркировка | [FFG_ndfl.py:1519-1628](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1519-L1628) |
| Добавление ID при покрытии шорта | [FFG_ndfl.py:361-364](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L361-L364) |
| Шаблон отображения | [upload.html:235-244](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\templates\reports_to_ndfl\upload.html#L235-L244) |
| CSS стили | [upload.html:75-100](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\templates\reports_to_ndfl\upload.html#L75-L100) |

---

# Часть 2: Детальное объяснение

## Шаг 0: Установка релевантности (выполняется ПЕРВЫМ!)

**Код:** [FFG_ndfl.py:1438-1517](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1438-L1517)

Для каждой сделки устанавливается флаг `is_relevant_for_target_year`:

1. **Продажи целевого года** (например, 2025):
   - `is_relevant_for_target_year = True`
   - Собираются их `used_buy_ids` в множество `used_buy_ids_for_target_year`

2. **Покупки**:
   - Если их `trade_id` есть в `used_buy_ids_for_target_year` → релевантны
   - Для агрегированных: проверяется каждый `original_trade_id`

3. **Остальные** → `is_relevant_for_target_year = False`

---

## Шаг 1: Создание словаря релевантности

**Код:** [FFG_ndfl.py:1526-1542](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1526-L1542)

```python
trade_id_to_relevant = {}
for каждой сделки:
    trade_id_to_relevant[trade_id] = is_relevant_for_target_year

    # Для агрегированных сделок
    if is_aggregated:
        for orig_id in original_trade_ids:
            # Оригинальные ID наследуют релевантность агрегированной сделки
            trade_id_to_relevant[orig_id] = is_relevant_for_target_year
```

**Зачем:** Быстрая проверка релевантности покупки по её ID, особенно для агрегированных сделок.

---

## Шаг 2: Создание цветных пар (покупка → продажа)

**Код:** [FFG_ndfl.py:1544-1576](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1544-L1576)

```python
pair_to_color = {}  # {(buy_id, sell_id): color}
color_index = 0
available_colors = ['#4FC3F7', '#FF9800', '#66BB6A', '#AB47BC', ...]

for каждой ПРОДАЖИ:
    if NOT sell.is_relevant_for_target_year:
        continue  # Пропускаем нерелевантные продажи

    for buy_id in sell.used_buy_ids (с удалением дубликатов):
        buy_is_relevant = trade_id_to_relevant.get(buy_id)

        if buy_is_relevant AND sell_is_relevant:
            # Создаём пару только если ОБЕ сделки релевантны
            pair_to_color[(buy_id, sell_id)] = available_colors[color_index % 8]
            color_index += 1
```

**Пример:**
```
Продажа #544230223 (2025, релевантна):
  used_buy_ids = [540972845, 546209854]

Пары:
  (540972845 → 544230223) → #FF9800 (оранжевый)
  (546209854 → 544230223) → #66BB6A (зелёный)

Продажа #597638009 (2025, релевантна):
  used_buy_ids = [546209854]

Пары:
  (546209854 → 597638009) → #AB47BC (фиолетовый)
```

**Важно:** Покупка 546209854 участвует в ДВУХ разных парах с РАЗНЫМИ цветами!

---

## Шаг 3: Назначение цветов покупкам

**Код:** [FFG_ndfl.py:1578-1604](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1578-L1604)

```python
trade_id_to_colors = {}  # {trade_id: [color1, color2, ...]}

for каждой ПОКУПКИ:
    if is_aggregated:
        # Для агрегированной покупки собираем цвета всех оригинальных сделок
        for orig_id in original_trade_ids:
            for (buy_id, sell_id), color in pair_to_color.items():
                if buy_id == orig_id:
                    colors.append(color)  # без дубликатов
    else:
        # Для обычной покупки ищем все пары (buy_id, *)
        for (buy_id, sell_id), color in pair_to_color.items():
            if buy_id == this_buy_id:
                colors.append(color)

    if colors:
        trade_id_to_colors[buy_id] = colors
```

**Пример:**
```
Покупка #540972845:
  Пары: (540972845 → 544230223)
  Цвета: [#FF9800] - 1 квадратик

Покупка #546209854:
  Пары: (546209854 → 544230223), (546209854 → 597638009)
  Цвета: [#66BB6A, #AB47BC] - 2 квадратика!
```

---

## Шаг 4: Назначение цветов продажам

**Код:** [FFG_ndfl.py:1606-1619](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1606-L1619)

```python
for каждой ПРОДАЖИ:
    # Ищем все пары (*, sell_id)
    for (buy_id, sell_id), color in pair_to_color.items():
        if sell_id == this_sell_id:
            colors.append(color)

    if colors:
        trade_id_to_colors[sell_id] = colors
```

**Пример:**
```
Продажа #544230223:
  Пары: (540972845 → 544230223), (546209854 → 544230223)
  Цвета: [#FF9800, #66BB6A] - 2 квадратика!

Продажа #597638009:
  Пары: (546209854 → 597638009)
  Цвета: [#AB47BC] - 1 квадратик
```

---

## Шаг 5: Присвоение цветов сделкам

**Код:** [FFG_ndfl.py:1621-1628](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1621-L1628)

```python
for каждой сделки:
    details['link_colors'] = trade_id_to_colors.get(trade_id, [])
```

---

## Отображение в шаблоне

**Код:** [upload.html:235-244](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\templates\reports_to_ndfl\upload.html#L235-L244)

```django
<td class="quantity-cell">
    <span class="link-indicators">
        {% if event.link_colors and event.is_relevant_for_target_year %}
            {% for color in event.link_colors %}
                <span class="link-indicator" style="background-color: {{ color }};"></span>
            {% endfor %}
        {% endif %}
    </span>
    <span class="quantity-value">{{ event.q|default:"-" }}</span>
</td>
```

**CSS:** [upload.html:75-100](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\templates\reports_to_ndfl\upload.html#L75-L100)

- `.quantity-cell` - flexbox контейнер (квадратики слева, число справа)
- `.link-indicators` - контейнер квадратиков с отступом 4px между ними
- `.link-indicator` - квадратик 12x12px с border и border-radius

---

# Часть 3: Важные детали и edge cases

## Шорты (короткие продажи)

ID будущей покупки добавляется в `used_buy_ids` продажи:

**Код:** [FFG_ndfl.py:361-364](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L361-L364)

```python
# Добавляем ID текущей покупки в used_buy_ids продажи, которая была шортом
if 'used_buy_ids' not in original_short_trade_ref:
    original_short_trade_ref['used_buy_ids'] = []
original_short_trade_ref['used_buy_ids'].append(op.get('trade_id'))
```

**Результат:** Продажа-шорт получает два цвета:
- Для прошлой покупки (частичное покрытие)
- Для будущей покупки (покрытие шорта)

### Пример: DQ.US

**Транзакции:**
```
1. 2025-05-27: Покупка #540972845 (1 акция)
2. 2025-06-06: Продажа #544230223 (2 акции) - использует:
   - 1 акцию из покупки #540972845
   - 1 акцию в шорт (покрывается будущей покупкой #546209854)
3. 2025-06-13: Покупка #546209854 (2 акции)
4. 2025-12-08: Продажа #597638009 (1 акция) - использует покупку #546209854
```

**Как формируется used_buy_ids для продажи #544230223:**
```python
# При FIFO обработке:
used_buy_ids.append(540972845)  # из прошлой покупки

# Когда будущая покупка #546209854 покрывает шорт (строки 361-364):
original_short_trade_ref['used_buy_ids'].append(546209854)

# Итого: used_buy_ids = [540972845, 546209854]
```

**Цветные пары:**
```
(540972845 → 544230223) → #FF9800 (оранжевый)
(546209854 → 544230223) → #66BB6A (зелёный)
(546209854 → 597638009) → #AB47BC (фиолетовый)
```

**Результат:**
```
Покупка #540972845:  [🟧] 1
Продажа #544230223:  [🟧][🟩] 2    ← два цвета!
Покупка #546209854:  [🟩][🟪] 2    ← два цвета!
Продажа #597638009:  [🟪] 1
```

---

## Агрегированные сделки

**Сценарий:**
```
1. Сделки A, B, C агрегируются → новый ID 'AGG_123'
2. original_trade_ids = ['A', 'B', 'C']
3. Продажа использует ID 'A' в used_buy_ids
```

**Маппинг цветов работает:**
```python
# Шаг 1: создаём словарь
for orig_id in ['A', 'B', 'C']:
    trade_id_to_relevant[orig_id] = True

# Шаг 2: создаём пары
buy_is_relevant = trade_id_to_relevant.get('A')  # True ✓
pair_to_color[('A', 'SELL_789')] = color

# Шаг 3: назначаем цвета покупкам
for orig_id in ['A', 'B', 'C']:
    if buy_id == orig_id:  # buy_id == 'A'
        colors.append(color)
trade_id_to_colors['AGG_123'] = colors  # Агрегированная получает цвет ✓
```

---

## Дедупликация used_buy_ids

При агрегации нескольких продаж:
```python
all_used_buy_ids.extend(detail_item['used_buy_ids'])
```

Если обе продажи использовали одну и ту же покупку, buy_id появится дважды.

**Решение:** Дедупликация в Шаге 2 (строки 1560-1565):
```python
seen_buy_ids = set()
unique_buy_ids = []
for buy_id in used_buy_ids:
    if buy_id not in seen_buy_ids:
        seen_buy_ids.add(buy_id)
        unique_buy_ids.append(buy_id)
```

---

## Палитра цветов

```python
available_colors = [
    '#4FC3F7',  # Голубой (Light Blue)
    '#FF9800',  # Оранжевый (Orange)
    '#66BB6A',  # Зелёный (Green)
    '#AB47BC',  # Фиолетовый (Purple)
    '#EF5350',  # Красный (Red)
    '#FFEB3B',  # Жёлтый (Yellow)
    '#26C6DA',  # Циан (Cyan)
    '#FF7043',  # Глубокий оранжевый (Deep Orange)
]
```

Цвета циклически повторяются через `color_index % 8`.

---

# Часть 4: Ограничения и Edge Cases

## 1. Зависимость от `final_instrument_event_history`

Словарь `trade_id_to_relevant` строится **только** по событиям с `display_type == 'trade'`:

```python
for isin_key, event_list_for_isin in final_instrument_event_history.items():
    for event_wrapper in event_list_for_isin:
        if event_wrapper.get('display_type') == 'trade':  # ← только сделки!
            # добавляем в trade_id_to_relevant
```

### Проблема:
- Если в `used_buy_ids` попадут ID начальных остатков (`display_type == 'initial_holding'`)
- Или ID покупок, отсутствующих в `final_instrument_event_history`
- То `trade_id_to_relevant.get(buy_id, False)` вернёт `False`
- И пара не будет создана, даже если покупка релевантна!

### Пример:
```python
# Продажа использует начальный остаток
used_buy_ids = ['INITIAL_123', 'TRADE_456']

# При создании пар:
buy_is_relevant = trade_id_to_relevant.get('INITIAL_123', False)  # False!
# Пара не создаётся → квадратик не показывается
```

### Решение (если нужно):
```python
# Было:
if event_wrapper.get('display_type') == 'trade':

# Станет:
if event_wrapper.get('display_type') in ['trade', 'initial_holding']:
    trade_id_to_relevant[trade_id] = is_relevant
```

---

## 2. Начальные остатки НЕ участвуют в цветовой маркировке

**Текущее поведение:**
- Начальные остатки (`display_type == 'initial_holding'`) НЕ добавляются в `trade_id_to_relevant`
- Даже если продажа использует начальный остаток, цвет для этой пары не создаётся

**Если нужно показывать цвета для начальных остатков:**

1. Расширить словарь релевантности:
```python
if event_wrapper.get('display_type') in ['trade', 'initial_holding']:
    details = event_wrapper.get('event_details')
    # существующая логика
```

2. Расширить Шаги 2 и 3:
```python
# Было:
if details and details.get('operation', '').lower() == 'buy':

# Станет:
if (details and details.get('operation', '').lower() == 'buy') or \
   event_wrapper.get('display_type') == 'initial_holding':
```

---

## 3. Конвертации помечаются по-особому

Из кода ([FFG_ndfl.py:1504-1514](c:\Users\_batman_\Desktop\NDFL\reports_to_ndfl\FFG_ndfl.py#L1504-L1514)):

```python
elif event_wrapper.get('display_type') == 'conversion_info':
    corp_action_id = details.get('corp_action_id')
    if corp_action_id and corp_action_id.startswith('CONV_IN_'):
        if corp_action_id in used_buy_ids_for_target_year:
            details['is_relevant_for_target_year'] = True
        else:
            details.setdefault('is_relevant_for_target_year', False)
    else:
        # Пока помечаем все конвертации как потенциально релевантные
        details.setdefault('is_relevant_for_target_year', True)
```

**Особенность:**
- Конвертации БЕЗ `corp_action_id` или без префикса `CONV_IN_` → всегда релевантны
- Но конвертации не участвуют в цветовой маркировке (только `display_type == 'trade'`)

---

## 4. Агрегированные сделки: риск потери маппинга

**Работает корректно**, если `original_trade_ids` сохранены.

Но если `original_trade_ids` не сохранены или не мапятся правильно → цвета теряются.

---

## Резюме ограничений

| Сценарий | Поведение | Критично? |
|----------|-----------|-----------|
| `used_buy_ids` содержит ID начального остатка | Цвет не создаётся | Средняя важность |
| `used_buy_ids` содержит ID вне `final_instrument_event_history` | Цвет не создаётся | Средняя важность |
| Начальный остаток используется в продаже | Квадратик не показывается | Низкая (по дизайну?) |
| Агрегированная покупка без `original_trade_ids` | Цвета теряются | Высокая (но не должно случиться) |
| Конвертация без `CONV_IN_` | Всегда релевантна (но не участвует в цветах) | Низкая |

---

## Отладка

Добавить после Шага 1 для проверки пропущенных buy_ids:
```python
for isin_key, event_list_for_isin in final_instrument_event_history.items():
    for event_wrapper in event_list_for_isin:
        if event_wrapper.get('display_type') == 'trade':
            details = event_wrapper.get('event_details')
            if details and details.get('operation', '').lower() == 'sell':
                used_buy_ids = details.get('used_buy_ids', [])
                for buy_id in used_buy_ids:
                    if buy_id not in trade_id_to_relevant:
                        print(f"WARNING: buy_id {buy_id} not in trade_id_to_relevant!")
```

---

# Часть 5: История исправлений

## Проблема #1: Цвета исчезли полностью

**Причина:** Цветовая маркировка выполнялась ДО установки `is_relevant_for_target_year`

**Решение:** Переместили блок цветовой маркировки (строки 1519-1628) ПОСЛЕ установки релевантности (строка 1517)

---

## Проблема #2: Актуальные сделки связаны с неактуальными

**Описание:** Когда актуальные сделки (2025) связаны с неактуальными сделками (2023-2024), цветные квадратики показывались для пар, где одна из сделок не относится к целевому году.

**Пример CCBN.KZ:**

До исправления:
- Покупки 2023 года (серые, неактуальные) → имели квадратики, связывающие с продажами 2025
- Продажа 2025 года → имела квадратики, связывающие с покупками 2023/2024

После исправления:
- Серые строки (не целевого года) → без квадратиков
- Актуальные строки → квадратики только для связей с другими актуальными строками

**Решение:** Добавлена фильтрация при создании пар (строки 1554-1556):
```python
# Пропускаем продажи, не относящиеся к целевому году
if not sell_is_relevant:
    continue
```

И проверка релевантности покупки (строки 1568-1572):
```python
buy_is_relevant = trade_id_to_relevant.get(buy_id, False)

# Создаем пару только если ОБЕ сделки релевантны
if buy_is_relevant and sell_is_relevant:
    # создаём пару
```

---

## Проблема #3: Слишком много квадратиков на покупках

**Описание:** Покупка показывала несколько одинаковых квадратиков, если использовалась в нескольких продажах.

**Причина:** Каждая пара (покупка → продажа) получала новый цвет, даже если покупка была та же самая.

**Решение:** Изменена логика - теперь каждая уникальная ПАРА получает свой цвет. Если покупка используется в нескольких продажах, она получает несколько РАЗНЫХ цветов.

---

## Проблема #4: Дубликаты цветов в агрегированных продажах

**Описание:** Агрегированная продажа показывала несколько одинаковых квадратиков.

**Причина:** `used_buy_ids` содержал дубликаты при агрегации.

**Решение:** Добавлена дедупликация (строки 1560-1565):
```python
seen_buy_ids = set()
unique_buy_ids = []
for buy_id in used_buy_ids:
    if buy_id not in seen_buy_ids:
        seen_buy_ids.add(buy_id)
        unique_buy_ids.append(buy_id)
```

---

## Проблема #5: Агрегированные покупки без цветов

**Описание:** Агрегированная покупка "Агрегировано (3): 356473681, 356473682, 356473789" не показывала квадратики.

**Причина:** Продажи использовали оригинальные ID в `used_buy_ids`, но агрегированная покупка имела новый ID.

**Решение:** Добавлен маппинг через `original_trade_ids` (строки 1254-1257, 1585-1594):
```python
# При создании словаря релевантности
if details.get('is_aggregated') and 'original_trade_ids' in details:
    for orig_id in details['original_trade_ids']:
        trade_id_to_relevant[orig_id] = is_relevant

# При назначении цветов покупкам
if details.get('is_aggregated') and 'original_trade_ids' in details:
    for orig_id in original_trade_ids:
        for (buy_id, sell_id), color in pair_to_color.items():
            if buy_id == orig_id and color not in colors:
                colors.append(color)
```

---

## Проблема #6: Шорты не получали два цвета

**Описание:** Продажа-шорт показывала только один цвет (для прошлой покупки), но не для будущей покупки, покрывающей шорт.

**Решение:** Добавлен код для записи ID будущей покупки в `used_buy_ids` продажи-шорта (строки 361-364).

---

# Итого

Система цветной маркировки работает следующим образом:

1. ✅ Каждая пара (покупка → продажа) получает уникальный цвет
2. ✅ Покупка может иметь несколько цветов (если используется в нескольких продажах)
3. ✅ Продажа может иметь несколько цветов (если использует несколько покупок)
4. ✅ Работает для шортов (ID будущей покупки добавляется в used_buy_ids)
5. ✅ Работает для агрегированных сделок (маппинг через original_trade_ids)
6. ✅ Фильтрует по релевантности (только актуальные пары показываются)
7. ✅ Правильный порядок выполнения (is_relevant → цвета)

**Ограничения:**
- Начальные остатки не участвуют (по дизайну, можно расширить)
- Зависит от наличия сделок в `final_instrument_event_history`
